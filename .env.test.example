# Copie para .env.test e preencha. Este arquivo é versionado; o .env.test não.
# O nome do banco precisa terminar em _test: a suíte se recusa a rodar fora disso.
DATABASE_URL=postgresql://postgres:TROQUE_ESTA_SENHA@localhost:5432/estudo_test

# Gere um novo, diferente do de desenvolvimento:
# node -pe "require('crypto').randomBytes(32).toString('hex')"
JWT_SECRET=

CORS_ORIGINS=http://localhost:5173
NODE_ENV=test
